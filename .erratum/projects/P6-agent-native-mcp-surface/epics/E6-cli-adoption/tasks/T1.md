# PT-P6-E6-T1 · `OutputFolderRoots.resolved(explicit:)`

**Epic:** PT-P6-E6

A non-menubar caller (the CLI) has no `MenuBarSettings` to read output folders from. This adds an
engine helper that resolves the roots a speaker edit should rewrite: the explicit `--output-folder`
roots when given, otherwise the default `~/Documents/PulsarTrace` (PT-P6-R9, PT-P6-D7).

## Acceptance criteria

- `OutputFolderRoots.resolved(explicit:)` returns the explicit roots when non-empty, else
  `[OutputFolderRoots.defaultRoot]`.
- `OutputFolderRoots.defaultRoot` is `~/Documents/PulsarTrace`.

## Files

- Create: `Sources/PulsarTraceEngine/Support/OutputFolderRoots.swift`
- Test: `Tests/UnitTests/OutputFolderRootsTests.swift`

- [ ] **Step 1: Write the failing test**

Create `Tests/UnitTests/OutputFolderRootsTests.swift`:

```swift
import Testing
import Foundation
@testable import PulsarTraceEngine

@Suite("OutputFolderRoots")
struct OutputFolderRootsTests {
    @Test("resolves explicit roots, else the default")
    func resolution() {
        let a = URL(fileURLWithPath: "/tmp/a"), b = URL(fileURLWithPath: "/tmp/b")
        #expect(OutputFolderRoots.resolved(explicit: [a, b]) == [a, b])
        #expect(OutputFolderRoots.resolved(explicit: []) == [OutputFolderRoots.defaultRoot])
        #expect(OutputFolderRoots.defaultRoot.lastPathComponent == "PulsarTrace")
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `swift test --filter OutputFolderRoots` (bare, `dangerouslyDisableSandbox: true`)
Expected: FAIL — `OutputFolderRoots` not defined.

- [ ] **Step 3: Implement the helper**

Create `Sources/PulsarTraceEngine/Support/OutputFolderRoots.swift`:

```swift
import Foundation

/// Resolve the output-folder roots a non-menubar caller (the CLI) should scan
/// when rewriting transcripts (PT-P6-R9, PT-P6-D7).
public enum OutputFolderRoots {

    /// `~/Documents/PulsarTrace` — the product default output folder.
    public static var defaultRoot: URL {
        FileManager.default.urls(for: .documentDirectory, in: .userDomainMask)[0]
            .appendingPathComponent("PulsarTrace", isDirectory: true)
    }

    /// Explicit roots when given, otherwise the single default root.
    public static func resolved(explicit: [URL]) -> [URL] {
        explicit.isEmpty ? [defaultRoot] : explicit
    }
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `swift test --filter OutputFolderRoots`
Expected: PASS — `resolution`.

- [ ] **Step 5: Commit**

```bash
git add Sources/PulsarTraceEngine/Support/OutputFolderRoots.swift Tests/UnitTests/OutputFolderRootsTests.swift
git commit -m "feat(engine): OutputFolderRoots.resolved for non-menubar callers (PT-P6-R9)"
```
