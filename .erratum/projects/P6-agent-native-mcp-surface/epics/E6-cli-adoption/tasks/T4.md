# PT-P6-E6-T4 · Finalize the operations manual

**Epic:** PT-P6-E6

A closing review of the operations manual now that every tool exists (PT-P6-R8, PT-P6-D5). Confirm it
documents the full shipped surface, describes PulsarTrace on its own terms, and names no external
program, service, or workflow. The manual was authored in PT-P6-E5-T3; this task is the
final-wording pass and the gate that it stays external-system-free.

## Acceptance criteria

- Every shipped tool (the 17 from PT-P6-E5-T4) is represented in the manual's reading / speaker /
  recording / discovery sections.
- The manual names no external program, service, or workflow.
- The `ManualTool` test passes.

## Files

- Modify: `Sources/PulsarTraceMCP/Resources/manual.md`
- Test (unchanged, run as the gate): `Tests/MCPTests/ManualToolTests.swift`

- [ ] **Step 1: Review the manual against the shipped surface**

Open `Sources/PulsarTraceMCP/Resources/manual.md` and check each shipped tool name appears and reads
correctly:

- Reading: `list_recordings`, `get_recording_meta`, `list_speakers`, `get_speaker`, `recent_events`.
- Speakers: `rename_speaker`, `merge_speakers`, `unmerge_speakers`, `split_speaker`, `unsplit_speaker`,
  `delist_speaker`, `undelist_speaker`, `delete_speaker`, `undelete_speaker`.
- Recordings: `rename_recording`, `request_refine`.
- Discovery: `manual`.

Tighten any wording that drifted during E3–E5 (tool names, argument names, reversibility notes). Do
not name any external program, service, or workflow — describe only PulsarTrace.

- [ ] **Step 2: Run the gate test**

Run: `swift test --filter ManualTool` (bare, `dangerouslyDisableSandbox: true`)
Expected: PASS — including the denylist assertion that no external system is named.

- [ ] **Step 3: Commit (only if the manual changed)**

```bash
git add Sources/PulsarTraceMCP/Resources/manual.md
git commit -m "docs(mcp): finalize the operations manual wording (PT-P6-R8)"
```

> With this task complete, every epic of P6 has working, tested (or build-and-smoke-verified)
> software. The project is ready for Erratum close-out: mint the product requirements
> (`PT-R115`+), the `PT-C22 · MCP Server` and `Speaker Edit Service` components, re-point the
> `// PT-P6-R*` code links to their product ids, and write the matrix rows. Run close-out per the
> `erratum` skill's `references/close-out.md`.
