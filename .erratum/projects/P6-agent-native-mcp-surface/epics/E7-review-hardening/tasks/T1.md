# PT-P6-E7-T1 · Negative Content-Length guard

**Epic:** PT-P6-E7

Harden `LoopbackHTTPRequest.parse` against a negative `Content-Length`, which reached
`Data.prefix(-1)` and trapped the process.

- [x] **Failing test.** In `Tests/MCPTests/LoopbackHTTPListenerTests.swift`, parse a raw
  `POST … Content-Length: -1 …` buffer and `#expect(… == nil)`. Verified RED: the process aborts
  with `Fatal error: Can't take a prefix of negative length`.
- [x] **Guard.** In `LoopbackHTTPRequest.parse`, add `guard expected >= 0 else { return nil }` ahead
  of the `available.prefix(expected)` slice. Verified GREEN: `swift test --filter MCPTests` — 36
  tests pass.
