# PT-P7-E6-T2 · Cross-links from the smoke checklist and the development doc

**Epic:** PT-P7-E6

One line in each neighbor document so the runbook is discoverable exactly where a release walk or a
dev-host setup starts (PT-P7-R8). No content duplication — links only.

## Acceptance criteria

- `docs/release-smoke-test.md` intro mentions the agent-assisted option with a link.
- `docs/development.md` links the runbook from its testing material.

## Files

- Modify: `docs/release-smoke-test.md`

- Modify: `docs/development.md`

- [ ] **Step 1: Add the links**

In `docs/release-smoke-test.md`, after the automation-marker note added by PT-P7-E3-T5:

```markdown
Unmarked UI items can also be walked by an AI agent — see
[docs/agent-verification.md](agent-verification.md).
```

In `docs/development.md`, at the end of the "UI end-to-end tests" section:

```markdown
For the agent-driven walk of the remaining manual checklist items, see
[docs/agent-verification.md](agent-verification.md).
```

- [ ] **Step 2: Format check and commit**

Run: `pre-commit run --files docs/release-smoke-test.md docs/development.md` Expected: pass.

```bash
git add docs/release-smoke-test.md docs/development.md
git commit -m "docs: cross-link the agent verification runbook (PT-P7-R8)"
```
