# PT-P7-E3-T5 · Mark automated items in `docs/release-smoke-test.md`

**Epic:** PT-P7-E3

The checklist stays authoritative (PT-R69); automation shrinks the human walk. Every item the E2/E3
suites now cover gets an inline automation marker naming its suite, so a release smoke run skips
exactly what a green UI run already proved (PT-P7-R4).

## Acceptance criteria

- Each covered item carries the marker format `*(automated: PulsarTraceUITests/<Suite>)*` appended
  to its line; uncovered items untouched.
- A short note under the document's intro explains the marker and the command that runs the suites.
- The checkbox itself stays — a marked item can still be walked by hand when desired.

## Files

- Modify: `docs/release-smoke-test.md`

- [ ] **Step 1: Add the marker note**

Under the document's intro, add:

```markdown
Items marked *(automated: …)* are covered by the UI end-to-end suites — a green
`scripts/run-ui-tests.sh` run stands in for walking them manually. Everything unmarked is still a
by-hand check.
```

- [ ] **Step 2: Mark the covered items**

Append the marker to exactly these items (menubar-UI section, lines 42–92 at the time of planning —
re-locate by text, the line numbers will have drifted):

- Settings persistence (mic/model/output folder/system-audio) across relaunch →
  `*(automated: PulsarTraceUITests/SettingsPersistenceTests)*`
- Live popover shows styled rows in real time →
  `*(automated: PulsarTraceUITests/RecordFlowTests — live.md growth; visual styling stays manual)*`
- Stop → refine → recordings list picks up `final.md` →
  `*(automated: PulsarTraceUITests/RecordFlowTests)*`
- Speaker rename rewrites all past `final.md` + `.bak` files →
  `*(automated: PulsarTraceUITests/SpeakerFlowTests)*`
- Merge confirmation with rewrite count + undo toast restoring library and transcripts →
  `*(automated: PulsarTraceUITests/SpeakerFlowTests)*`
- Recordings list shows recordings / empty states for library and list →
  `*(automated: PulsarTraceUITests/FloorTests — seeded-content rendering; empty states stay manual)*`

Do **not** mark: icon-state matrix, hotkey behavior, notifications, TCC/DMG/Gatekeeper items,
sleep/wake, disk-full, crash recovery, ANE/performance items — those remain manual or belong to the
agent runbook (PT-P7-E6).

- [ ] **Step 3: Format check and commit**

Run: `pre-commit run --files docs/release-smoke-test.md` Expected: pass (re-stage after mdformat
rewraps if needed).

```bash
git add docs/release-smoke-test.md
git commit -m "docs: mark smoke-checklist items covered by the UI e2e suites (PT-P7-R4)"
```
