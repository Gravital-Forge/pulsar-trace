# PT-P7-E5-T2 · `docs/development.md` section

**Epic:** PT-P7-E5

Document the smoke next to the existing hardware-test setup it shares prerequisites with (PT-P7-R7).

## Acceptance criteria

- `docs/development.md` explains, in the hardware-test area: what the script proves beyond fixture
  mode, the prerequisites it shares with the device tests (BlackHole 2ch, mic TCC on the terminal)
  plus `switchaudio-osx`, the run command, and the relationship to `audio-loopback-check.sh`
  (environment diagnostic vs product test — one sentence each).

## Files

- Modify: `docs/development.md`

- [ ] **Step 1: Write the section**

Append after the existing hardware-test instructions:

```markdown
## App-level real-audio smoke

    scripts/e2e-audio-smoke.sh

Plays a committed voice sample through BlackHole into the real capture daemon and asserts the
refined transcript against the sample's reference — the one end-to-end path fixture-mode UI tests
cannot prove. Needs the same setup as the device tests (BlackHole 2ch, Microphone permission for
the terminal) plus `brew install switchaudio-osx`. State is isolated to a temp home; the default
output device is restored on exit.

`audio-loopback-check.sh` remains the narrower environment diagnostic (does audio route through
this host's loopback at all); the smoke is the product test on top of it.
```

- [ ] **Step 2: Format check and commit**

Run: `pre-commit run --files docs/development.md` Expected: pass.

```bash
git add docs/development.md
git commit -m "docs: real-audio smoke usage and prerequisites (PT-P7-R7)"
```
