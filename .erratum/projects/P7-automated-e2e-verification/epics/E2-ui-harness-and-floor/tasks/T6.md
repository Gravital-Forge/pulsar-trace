# PT-P7-E2-T6 · `docs/development.md`: UI end-to-end testing section

**Epic:** PT-P7-E2

Document the harness for a dev-host user (PT-P7-R4/R5 documentation duty). One home per fact: the
identifier convention's home is the `A11yID.swift` header — this section links to it rather than
restating it.

## Acceptance criteria

- `docs/development.md` gains a "UI end-to-end tests" section: prerequisites (full Xcode,
  `brew install xcodegen`), the one-command run, how to run a single suite, where result bundles
  land, and the isolation guarantee (each run seeds a throwaway home; the daily app can stay
  running; add `PULSARTRACE_MODELS_DIR` to share the model cache when a flow needs models).
- No restated convention, no restated project internals — links to `project.yml`,
  `scripts/run-ui-tests.sh`, and `Sources/PulsarTraceMenuBar/A11yID.swift`.

## Files

- Modify: `docs/development.md`

- [ ] **Step 1: Write the section**

Append to `docs/development.md`:

```markdown
## UI end-to-end tests

XCUITest drives the real menubar app through the accessibility tree. The suite lives in
`UITests/`; the disposable Xcode project that hosts it is generated from `project.yml`
(never committed).

Prerequisites: full Xcode (not just the CLT) and `brew install xcodegen`.

Run everything:

    scripts/run-ui-tests.sh

One suite (arguments pass through to xcodebuild):

    scripts/run-ui-tests.sh -only-testing:PulsarTraceUITests/FloorTests

Result bundles land in `.build/ui-test-results/`.

Each test launches the app with `PULSARTRACE_HOME` and `PULSARTRACE_DEFAULTS_SUITE` pointing at a
throwaway seeded home, so runs never touch your real settings, speaker library, recordings, or
events — the daily app can stay running (a second menu-bar icon appears during the run). Flow
tests that exercise recording set the fixture variables and add `PULSARTRACE_MODELS_DIR` to reuse
your existing model cache instead of re-downloading.

Element lookup is by accessibility identifier only; the convention and the full identifier list
live in `Sources/PulsarTraceMenuBar/A11yID.swift`.
```

- [ ] **Step 2: Format check**

Run: `pre-commit run --files docs/development.md` Expected: hooks pass (mdformat may rewrap;
re-stage if it does).

- [ ] **Step 3: Commit**

```bash
git add docs/development.md
git commit -m "docs: UI end-to-end test harness usage (PT-P7-R4)"
```
