# PulsarTrace events log schema

The events log is a public API surface consumed by external tools and agents. Its normative
specification lives with the architecture:

→ **`.erratum/product/architecture/events-log.md`** (the `PT-C6 · Events Log` contract).

Format changes follow per-type versioning (PT-R81, PT-R85).
